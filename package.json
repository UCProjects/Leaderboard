{
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start": "node index.js",
    "host": "bundle exec jekyll serve",
    "build": "bundle install && bundle exec jekyll build"
  },
  "engines": {
    "node": "^12.0.0"
  },
  "author": "feildmaster",
  "license": "MIT",
  "dependencies": {
    "needle": "^2.5.2"
  },
  "devDependencies": {
    "@actions/exec": "^1.0.4",
    "fs-extra": "^9.0.1"
  }
}
